# Pomelo GPT 项目规则

## 文档记录规则

每当完成一个功能需求或重要修改时，必须在 `docs/` 目录下创建或更新对应的 Markdown 文档来记录：

1. **功能描述**：简要说明实现的功能
2. **实现细节**：关键代码位置和实现逻辑
3. **修改的文件**：列出所有修改的文件
4. **使用方法**：如何使用该功能
5. **注意事项**：需要注意的问题或限制

## 文档命名规范

- 使用日期前缀：`YYYY-MM-DD-功能名称.md`
- 或使用功能分类：`feature-功能名称.md`
- 使用中文或英文，保持一致性

## 示例文档结构

```markdown
# 功能名称

**完成日期**：YYYY-MM-DD

## 功能描述
简要描述实现的功能

## 实现细节
- 修改的文件列表
- 关键代码说明
- 技术选型

## 使用方法
如何使用该功能

## 注意事项
需要注意的问题
```

## 代码规范

- 使用 TypeScript 严格模式
- 遵循 ESM 模块规范
- 使用 Vue 3 Composition API
- 保持代码简洁，避免过度工程化

## 测试规范

每个功能模块都应该编写对应的单元测试：

1. **测试文件位置**：`src/__tests__/` 目录
2. **测试文件命名**：`功能名.test.ts` 或 `组件名.spec.ts`
3. **注释要求**：每个测试套件和测试用例都必须包含详细的中文注释
4. **注释格式**：
   ```typescript
   /**
    * 测试套件描述
    * 说明这个测试套件的整体目的
    */
   describe('测试套件名称', () => {
     /**
      * 单个测试用例描述
      * 详细说明这个测试验证什么功能
      */
     it('测试用例名称', () => {
       // 测试代码
     })
   })
   ```
5. **测试原则**：
   - 测试应该快速、可靠、独立
   - 一个测试只验证一个功能点
   - 使用描述性的测试名称
   - 遵循 AAA 模式：Arrange（准备）、Act（执行）、Assert（断言）

## Node 版本管理规则

**重要**：在执行任何 npm 或 pnpm 命令之前，必须先执行 `nvm use` 确保使用正确的 Node 版本。

### 命令执行规范

```bash
# 错误示例 ❌
pnpm install

# 正确示例 ✅
source ~/.nvm/nvm.sh && nvm use && pnpm install

# 或者在 bash 中执行
bash -c 'source ~/.nvm/nvm.sh && nvm use && pnpm install'
```

### 适用场景

以下命令执行前都需要先运行 `nvm use`：
- `pnpm install` / `pnpm add` / `pnpm remove`
- `npm install` / `npm run`
- `pnpm dev` / `pnpm build` / `pnpm test`
- 任何其他 npm/pnpm 相关命令

